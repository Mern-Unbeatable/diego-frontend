export const selectAdmin = (state) => state.admin;

export const selectAdminDashboard = (state) => state.admin.dashboard;

export const selectAdminLicenses = (state) => state.admin.licenses;

export const selectAdminLicensePlans = (state) => state.admin.licensePlans;
